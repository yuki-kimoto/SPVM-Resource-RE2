use strict;
use warnings;
use SPVM::Builder::Config;

my $config = SPVM::Builder::Config->new_cpp(file => __FILE__);

$config->add_ccflags('-pthread', '-Wno-unused-parameter', '-Wno-missing-field-initializers');

$config->set_std('gnu++17');

$config->ext('cc');

$config;
